 <core:FragmentDefinition
	xmlns="sap.m"
    xmlns:u="sap.ui.unified"
	xmlns:core="sap.ui.core">
 <Dialog title="" class="sapUiContentPadding">
   
 	
        <VBox>
            <VBox width="100%">
               
               <Image id="image1" /> 
               
            </VBox>
           
        </VBox>
        <buttons>
            <Button id="closeDialog" text="{appTxts>/date.close}" press="onCloseImageDialog"/>
        </buttons>
        <buttons>
            <Button id="realizaConsulta" text="{appTxts>/notifications.save}" press="handleDownloadPress"/>
        </buttons>
        
    </Dialog>
</core:FragmentDefinition>