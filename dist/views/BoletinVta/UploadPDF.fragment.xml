<core:FragmentDefinition
	xmlns="sap.m"
    xmlns:u="sap.ui.unified"
	xmlns:core="sap.ui.core">
    <Dialog title="{appTxts>/foliosCap.titleUpload}" class="sapUiContentPadding">
            <HBox id="uploadBox" alignContent="Center" alignItems="Center">            
                <u:FileUploader
                    id="fileUploader"
                    name="myFileUpload"
                    tooltip="{appTxts>/helpDocs.uploader.tooltip}"
                    uploadComplete="handleUploadComplete"
                    change="onChangeFUP"
                    style="Emphasized"
                    fileType="{configSite>/updateFormatsSingle}"
                    placeholder="{appTxts>/helpDocs.uploader.placeholder}">
                        <u:parameters>
                            <u:FileUploaderParameter
                                name="Accept-CH"
                                value="Viewport-Width"/>
                            <u:FileUploaderParameter
                                name="Accept-CH"
                                value="Width"/>
                            <u:FileUploaderParameter
                                name="Accept-CH-Lifetime"
                                value="86400"/>
                        </u:parameters>
                    </u:FileUploader>
            </HBox>
        <buttons>
            <Button id="closeDialog" text="{appTxts>/foliosCap.close}" press="onCloseDialogUpload"/>
        </buttons>
    </Dialog>
</core:FragmentDefinition>