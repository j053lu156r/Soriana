<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core">
    <Dialog title="{appTxts>/rem.palletbox}" class="sapUiContentPadding">
        <core:HTML content='{/html}'>
		</core:HTML>
        <buttons>
            <Button id="closeDialog" text="{appTxts>/date.close}" press="onCloseDialogBoxPrint"/>
        </buttons>
        <buttons>
            <Button id="realizaConsulta" text="{appTxts>/rem.print}" press="saveCoordinates"/>
        </buttons>
	</Dialog>
</core:FragmentDefinition>